# SQLPlayground

Database design for a shopping application from scratch, ui that consists of database tables and textbox holds a sql query, spring rest api that takes a query and runs, hibernate for entity mapping
